{
  "name": "dudidesk",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "bundle": "esbuild src/main.ts --bundle --outfile=src/bundle.js --format=esm --sourcemap",
    "bundle:watch": "esbuild src/main.ts --bundle --outfile=src/bundle.js --format=esm --sourcemap --watch"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
